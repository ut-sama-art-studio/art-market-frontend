/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "art-market-bucket.tor1.digitaloceanspaces.com",
        ],
    },
};

export default nextConfig;
